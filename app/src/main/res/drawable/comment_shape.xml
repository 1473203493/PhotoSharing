<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/comment_input" /> <!-- 填充为灰色 -->
    <stroke
        android:width="1dp"
        android:color="@color/comment_input" /> <!-- 边框颜色和宽度 -->
    <corners android:radius="22dp" /> <!-- 圆角半径 -->
</shape>
