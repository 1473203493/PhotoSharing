<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/blue" /> <!-- 填充为灰色 -->
    <stroke
        android:width="1dp"
        android:color="@color/blue" /> <!-- 边框颜色和宽度 -->
    <corners android:radius="20dp" /> <!-- 圆角半径 -->
</shape>